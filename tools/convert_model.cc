//#include "mex.h"
//#include "model.h"


/*
 * Convert a Matlab pose estimation model to a C++ model
 * and save it to a file.
 */
//void mexFunction(int nlhs, mxArray *plhs[], int nrhs, const mxArray *prhs[])
//{
//	if (nrhs != 2)
//		mexErrMsgTxt("Wrong number of inputs");
//	if (nlhs != 0)
//		mexErrMsgTxt("Wrong number of outputs");

//	// get parameters
//	const mxArray *model = prhs[0];
//	const char *outputFileName = mxArrayToString(prhs[1]);

//	// convert Matlab model to C++ and save
//	Model *model_c = new Model;
//	init_model(model, model_c);
//	save_model(model_c, outputFileName);
//	delete_model(&model_c);
//}

